# Environment configuration

When adding, changing, or removing environment variables, update the root
`.env.example` in the same change. Document each variable's purpose, whether it is
required or optional, and where it is loaded. Use empty values or safe placeholders
and never include real credentials. Keep the README setup instructions in sync.
